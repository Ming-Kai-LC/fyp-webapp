"""
RESTful API Module - Models

This module doesn't define its own models.
It uses models from detection, appointments, and other apps.
"""

from django.db import models

# No models defined - API uses existing models from other apps
